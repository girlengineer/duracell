/**
 * @module CoreServicesModule
 **/
/** ContentService **/
import { Observable } from "rxjs";
import { Http } from '@angular/http';
import { AppConfigService } from '../services';
/**
 * Created by rg37616 on 11/28/2016.
 */
/**
 * The ContentService
 * @class ContentService
 **/
export declare class ContentService {
    private _http;
    private _appConfigService;
    private _currentContent;
    constructor(_http: Http, _appConfigService: AppConfigService);
    get(contentType: string, contentId: string, prop: string): any;
    mergeContentData(dynamicData: any, appConfigContent: any, bundleName: any): Observable<any>;
    private buildStaticServerUrl(appConfigContent, bundleName);
    private getStaticData(appConfigContent, bundleName);
    private mergeRecursive(obj1, obj2);
    private mergeDynamcicContentData(dat);
}
