import {TestBed} from '@angular/core/testing';

import {SliderComponent} from './slider.component';

describe('SliderComponent', () => {
    beforeEach(() => TestBed.configureTestingModule({ declarations: [SliderComponent] }));

    it('should instantiate the SliderComponent', () => {
        let fixture = TestBed.createComponent(SliderComponent);
        expect(fixture.componentInstance instanceof SliderComponent).toBe(true, 'should create SliderComponent');
    });
});
