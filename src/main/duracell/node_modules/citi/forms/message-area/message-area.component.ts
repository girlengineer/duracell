import { Component, OnInit, Input } from '@angular/core';

/**
 * @Component: Message Area
 * @Description: This is the message-area component and is used to create reusable message area
 * from Bootstrap.
*/
@Component({
  selector: 'citi-message-area',
  template: require('./message-area.component.html'),
  styles: [ require('./message-area.component.css') ]
})
export class MessageAreaComponent implements OnInit {
  /**
    * @Property: idStr 
    * @Description: This defines the id of the message area component
    * @Type: string 
   */
  @Input() idStr:string;
  /**
    * @Property: additionalClasses 
    * @Description: This defines the additional classes to the message area component
    * @Type: string 
   */
  @Input() additionalClasses: string;
  
  constructor() { }

  ngOnInit() {
  }

}
