import { EventEmitter } from "@angular/core";

export class DropdownService {

    public optionAdded: EventEmitter<any>;

    constructor() {
        this.optionAdded = new EventEmitter<any>();
    }

    addOption: Function = function( option: any ){

        this.optionAdded.emit(option);
    }
}