/* tslint:disable:no-unused-variable */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { ActionPrimaryMultipleRowComponent } from './action-primary-multiple-row.component';

describe('ActionPrimaryMultipleRow', () => {
  let component: ActionPrimaryMultipleRowComponent;
  let fixture: ComponentFixture<ActionPrimaryMultipleRowComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ActionPrimaryMultipleRowComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ActionPrimaryMultipleRowComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
