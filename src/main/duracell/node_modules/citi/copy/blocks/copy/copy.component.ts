import { Component, Input } from '@angular/core';

/**
 * @Component: Copy Section
 * @Description: This component generates a basic copy section (B1.0)
 */
@Component({
    selector: 'citi-copy-section',
    template: require('./copy.component.html')
})
export class CopyComponent {

    /**
     * @Property: title
     * @Description: The title for the copy section
     * @Type: String
     */
    @Input() title: string;
}