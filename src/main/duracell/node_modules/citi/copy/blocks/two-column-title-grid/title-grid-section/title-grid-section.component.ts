import { Component, Input } from '@angular/core';

/**
 * @Component: Two Column Title Grid Section
 * @Description: This component generates a two column title grid section (B1.6)
 */
@Component({
    selector: 'citi-title-grid-section',
    template: require('./title-grid-section.component.html')
})
export class TitleGridSectionComponent {

    /**
     * @Property: title
     * @Description: The title for the two column title grid section
     * @Type: String
     */
    @Input() title: string;
}