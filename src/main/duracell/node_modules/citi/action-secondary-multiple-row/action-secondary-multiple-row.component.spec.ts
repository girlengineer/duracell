/* tslint:disable:no-unused-variable */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { ActionSecondaryMultipleRowComponent } from './action-secondary-multiple-row.component';

describe('ActionSecondaryMultipleRow', () => {
  let component: ActionSecondaryMultipleRowComponent;
  let fixture: ComponentFixture<ActionSecondaryMultipleRowComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ActionSecondaryMultipleRowComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ActionSecondaryMultipleRowComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
