import { OnInit, EventEmitter } from '@angular/core';
import { TabComponent } from '../tab/tab.component';
/**
 * @Component: Tab-group
 * @Description: A wrapper for displaying individual tab panels
 */
export declare class TabGroupComponent implements OnInit {
    /**
     * @Property: id
     * @Description: ID attribute of the outer element
     * @Type: String
     */
    id: any;
    /**
     * @Property: activeTab
     * @Description: Which tab is initially visible (default is 1, the first tab)
     * @Type: Number
     */
    activeTab: any;
    /**
      * @Event: tabChanged
      * @Description: This event that is fired when a tab activates; returns the index of the active tab (first tab is 1)
      * @Return: Number
      */
    tabChanged: EventEmitter<any>;
    private tabs;
    defaultTab: number;
    addTab(tab: TabComponent): number;
    countTabs(): number;
    constructor();
    ngOnInit(): void;
    selectTab(tab: TabComponent, event: any): void;
    currentTab(): number;
}
