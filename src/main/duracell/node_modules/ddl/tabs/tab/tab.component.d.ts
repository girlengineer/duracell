import { OnInit } from '@angular/core';
import { TabGroupComponent } from '../tab-group/tab-group.component';
/**
 * @Component: Tab
 * @Description: Individual tab panel to be wrapped in a citi-tab-group
 */
export declare class TabComponent implements OnInit {
    private parent;
    /**
     * @Property: title
     * @Description: Text shown as the clickable tab
     * @Type: String
     */
    tabTitle: any;
    classTabPane: boolean;
    classLevel1: boolean;
    classParent: boolean;
    isExpanded: boolean;
    private tabgroup;
    private lastTab;
    tabIndex: number;
    constructor(parent: TabGroupComponent);
    ngOnInit(): void;
    selectTab(event: any): void;
}
