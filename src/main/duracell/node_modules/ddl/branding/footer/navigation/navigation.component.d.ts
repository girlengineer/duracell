import { ElementRef, QueryList, OnInit } from '@angular/core';
import { Utils } from '../../utils.class';
import { FormContainerComponent } from '../../../forms/form-container/form-container.component';
/**
 * @Component: Footer Navigation
 * @Section: Navigation
 * @Description: This component generates the navigation and location finder section of the footer.
 */
export declare class FooterNavigationComponent implements OnInit {
    private utils;
    private element;
    constructor(utils: Utils, element: ElementRef);
    locationFinder: ElementRef;
    formContainer: QueryList<FormContainerComponent>;
    config: any;
    sections: boolean[];
    isMobile: boolean;
    toggleSection: Function;
    getMobile: Function;
    submitLocationFinder: Function;
    ngOnInit(): void;
    setNavigationConfig: (_config: any) => void;
    /**
     * @Property: brandingConfig
     * @Description: This is a reference to the branding config JSON object retrieved from the branding API.
     * @Type: String
     */
    brandingConfig: any;
}
