import { Step } from './step';
import { EventEmitter } from "@angular/core";
export declare class Progress {
    private _steps;
    private _currentStep;
    onActiveStepChanged: EventEmitter<Step[]>;
    constructor(steps?: any, isFirstStepActive?: boolean);
    private setFinishedStepsToPosition(atPosition);
    private setNormalStepsFromPosition(atPosition);
    private setActiveStep();
    private isItemDeletable();
    addStep(newStep: Step, atPosition?: number, isCurrentStep?: boolean): void;
    removeAStep(stepIndex?: number): void;
    readonly hasSteps: boolean;
    readonly currentStep: Step;
    readonly numberOfSteps: number;
    currentStepIndex: number;
    steps: Step[];
    readonly hasActiveStep: boolean;
    readonly isAtLastStep: boolean;
    readonly isAtFirstStep: boolean;
    setStepActive(stepIndex: number): void;
    clearAllSteps(): void;
}
