import { OnChanges, SimpleChanges } from '@angular/core';
import { AbstractControl, Validator, ValidatorFn } from '@angular/forms';
/** A hero's name can't match the given regular expression */
export declare function forbiddenNameValidator(nameRe: RegExp): ValidatorFn;
export declare class ValidationDirective implements Validator, OnChanges {
    forbiddenName: string;
    private valFn;
    ngOnChanges(changes: SimpleChanges): void;
    validate(control: AbstractControl): {
        [key: string]: any;
    };
}
