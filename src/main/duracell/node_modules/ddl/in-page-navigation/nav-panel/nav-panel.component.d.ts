import { OnInit } from '@angular/core';
import { NavGroupComponent } from '../nav-group/nav-group.component';
/**
 * @Component: Nav Panel
 * @Section: Navigation
 * @Description: This is the nav panel component and is used to create panel in the nav group
 * from Bootstrap.
*/
export declare class NavPanelComponent implements OnInit {
    private parent;
    classTabPane: boolean;
    classLevel1: boolean;
    classParent: boolean;
    isExpanded: boolean;
    /**
      * @Property: title
      * @Description: This defines the title of the in page navigation
      * @Type: string
     */
    tabTitle: any;
    private tabgroup;
    private lastTab;
    tabIndex: number;
    constructor(parent: NavGroupComponent);
    ngOnInit(): void;
    selectTab(event: any): void;
}
