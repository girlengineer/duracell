"use strict";
var core_1 = require('@angular/core');
var ValidationService = (function () {
    function ValidationService() {
        this.errors = [];
        this.addError = function (error) {
            if (this.errors.indexOf(error) === -1) {
                this.errors.push(error);
                this.errorAdded.emit(error);
            }
        };
        this.removeError = function (error) {
            var index = this.errors.indexOf(error);
            if (index !== -1) {
                this.errors.splice(index, 1);
                this.errorRemoved.emit(error);
            }
        };
        this.validate = function () {
            this.validateNow.emit();
        };
        this.errorAdded = new core_1.EventEmitter();
        this.errorRemoved = new core_1.EventEmitter();
        this.validateNow = new core_1.EventEmitter();
    }
    return ValidationService;
}());
exports.ValidationService = ValidationService;
//# sourceMappingURL=validation.service.js.map