import { OnInit } from '@angular/core';
import { AccordionService } from "../accordion.service";
/**
 * @Component: Accordion
 * @Description: This component generates the wrapper, title, and expand/collapse
 * button for accordions. Sections for the accordion are a separate component.
 */
export declare class AccordionComponent implements OnInit {
    private service;
    constructor(service?: AccordionService);
    sections: any[];
    allSectionsOpen: Boolean;
    toggleAllSections: Function;
    checkIfAllSectionsAreOpen: Function;
    ngOnInit(): void;
    /**
     * @Property: title
     * @Description: The title displayed at the top of the accordion
     * @Type: String
     */
    title: string;
    /**
     * @Property: theme
     * @Description: Default theme is light. The value "dark" can be passed to this input to use the dark theme.
     * @Type: String
     */
    theme: string;
}
