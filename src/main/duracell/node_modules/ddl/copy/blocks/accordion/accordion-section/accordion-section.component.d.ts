import { ElementRef } from '@angular/core';
export declare class AccordionSectionComponent {
    private element;
    constructor(element: ElementRef);
    index: Number;
    ngOnInit(): void;
    title: string;
    description: string;
}
